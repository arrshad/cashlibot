# Bot
aiogram==3.13.1

# Web API
fastapi==0.115.6
uvicorn[standard]==0.34.0

# ORM / DB
sqlmodel==0.0.22
SQLAlchemy[asyncio]==2.0.36
asyncpg==0.30.0
alembic==1.14.0

# Settings & YAML
# pydantic pinned <2.10 because aiogram 3.13 hasn't bumped its ceiling yet.
pydantic==2.9.2
pydantic-settings==2.6.1
PyYAML==6.0.2

# Cache
redis[hiredis]==5.2.1

# AI orchestration — provider-agnostic via langchain factories.
langchain==0.3.10
langchain-openai==0.2.14
langchain-anthropic==0.3.1

# Semantic memory via pgvector (Postgres extension enabled in migration 0001).
pgvector==0.3.6
# pgvector transitively pulls in NumPy. NumPy 2.x is built with an X86_V2
# CPU baseline (SSE4.2/POPCNT), which some older/virtualised server CPUs
# don't advertise, causing an ImportError at boot. 1.26.x supports the
# older baseline and still works with Python 3.12 + pgvector 0.3.
numpy>=1.26,<2.0

# Scheduler for reminders (recurring transactions will reuse it later).
APScheduler==3.10.4
python-dateutil==2.9.0.post0

# JWT for the admin dashboard's Bearer auth.
PyJWT==2.10.1
